<table class='table table-hover table-striped'>
  <% Choices.each do |id, text| %>
    <tr>
      <th><%= text %></th>
      <td><%= @intel[id] || 0 %>
      <td><%= '#' * (@intel[id] || 0) %></td>
    </tr>
  <% end %>
</table>
<p><a href='/'>Redo Intel Preferences</a></p>